apiVersion: v1
kind: Service
metadata:
  name: payments-service
  namespace: ecommerce
spec:
  selector:
    app: payments-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
